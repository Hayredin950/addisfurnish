# Edge function secrets — copy to .env and fill, then:
#   supabase secrets set --env-file supabase/functions/.env

# Required by all functions (send-push, telegram-bot)
SUPABASE_URL=https://ssihhmhsptlbalidutqa.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Telegram bot (telegram-bot function only) — create via @BotFather
TELEGRAM_BOT_TOKEN=

# Optional — Expo push API scoping (https://expo.dev/settings/access-tokens)
EXPO_ACCESS_TOKEN=
